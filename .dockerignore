# Build caches / deps
# Build caches / deps
node_modules
.next
.next/cache
.turbo

# VCS
.git
.gitignore

# Local dev artifacts
Dockerfile.dev
docker-compose.debug.yml
docker-compose.yml

# Logs & OS
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.swp
.DS_Store
dist